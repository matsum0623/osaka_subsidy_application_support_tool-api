const { jwtDecode } = require('jwt-decode')

exports.Auth = {
  check_id_token: (event) => {
    if (!event || !event.headers || !event.headers.Authorization){
      return false
    }else {
      return jwtDecode(event.headers.Authorization);
    }
  }
}